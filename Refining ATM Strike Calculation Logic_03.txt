One final "Logical Bridge" missing: How the system handles the "ATM ±" calculation dynamically. If you don't define this logic now, the AI might write a static validator that doesn't work for different strike prices.
Here is the refined Master Logic Package for you to provide to your development tool.
________________
1. The "Dynamic Strike" Logic (The Bridge)
This is the most critical logic for your "Validate" button.
* Input: User selects NIFTY, ATM+, and 100.
* The Logic:
   1. Find the IndicatorData entry where Buy = 1.
   2. At that exact DateTime, query the Options_Data table for the "Index/Spot" price (where Type is perhaps marked as 'Spot' or by a specific script name).
   3. Calculation: TargetStrike = Round(SpotPrice) + 100.
   4. Lookup: Find the Options_Data record where Script matches that TargetStrike and the DateTime matches the signal.
* Refinement: You must ensure your Script naming convention in the DB is searchable (e.g., NIFTY_19500_CE).
2. The "Entry Point" Precision Logic
Your Indicator.csv form has "At Signal" vs "Next Candle."
* Logic for "Next Candle": If the signal is at 10:00 AM on a 5-minute chart, the Entry must be recorded at the Open price of the 10:05 AM candle.
* Flaw to fix: Ensure your backend doesn't just look for "Time + 1 minute" but looks for the very next available record in the DB, in case there are gaps in data.
________________
3. The Refined Workflow for "Antigravity" (Copy-Paste Ready)
Give this specific instruction to your development tool to ensure the code is architecturally sound:
System Architecture Requirements:
1. Data Ingestion: Create a parser that accepts CSV/XLSX. It must perform "Header Mapping" where the user assigns file columns to DB fields (DateTime, Open, High, Low, Close, Buy, Sell).
2. Pre-Storage Filter: Implement a date-range filter before Prisma.createMany(). Only store records between startDate and endDate.
3. Validation Engine: > - Perform a Join between IndicatorData and OptionsData using DateTime and Stock as the primary keys.
   * Implement ATM Offset math: Calculate the strike price based on the spot price at the time of the signal.
   * Handle Execution Logic: Support "Next Candle" entry by fetching the chronologically subsequent record.
4. Performance: Use FastAPI background tasks for the validation button to prevent frontend timeouts during large data processing.
________________
4. Final Architect's Checklist (Are you ready?)
Feature
	Status
	Requirement
	Prisma Schema
	Ready
	Use the one I provided in the previous turn.
	Field Mapping
	Ready
	User selects headers from Excel during upload.
	Range Filter
	Ready
	Filter at the moment of upload (Point #1).
	ATM Calculation
	Pending
	Ensure your Options_Data includes the "Spot Index" price so the calculator has a base to work from.
	











To ensure excellence and total precision in your SaaS, we must treat the "Spot Price" as the anchor for all calculations. Without it, the "ATM ±" feature cannot function.
Here is the refined architecture and logic for integrating Spot Price.
1. New Table: Spot_Data
I recommend a separate table for Spot prices. This keeps your Options_Data clean and makes queries 10x faster.
Updated Prisma Schema:
Code snippet
model SpotData {
  id        String   @id @default(uuid())
  stock     String   // e.g., "NIFTY", "BANKNIFTY"
  dateTime  DateTime
  date      DateTime @db.Date
  time      String
  price     Decimal  // The actual Spot Index price
  
  @@index([dateTime, stock])
}


________________


2. The "ATM Calculation" Logic
This is the math that "Antigravity" needs to implement for the "Validate" button.
The "Anchor & Offset" Algorithm:
1. Find Signal: User wants to validate a "Buy" signal at 2023-10-24 10:15:00.
2. Get Spot: System queries SpotData for NIFTY at 10:15:00. (Let’s say the price is 19,523.45).
3. Identify Base ATM: * Formula: Base = Round(Spot / Interval) * Interval
   * For Nifty (Interval 50): Round(19523.45 / 50) * 50 = 19,500.
4. Apply User Offset: * User selected ATM+ and 100.
   * Target Strike = 19,500 + 100 = 19,600.
5. Fetch Option: System now looks into Options_Data for Script containing 19600 and Type: Call at 10:15:00.
________________


3. Updated Workflow for Development (Refined for Antigravity)
When you send this to your developer/AI, use this refined logic block:
Feature: Spot-Based Option Mapping
1. Data Requirement: The system must allow importing a "Spot Price" CSV. Map this to the SpotData table.
2. Dynamic Strike Finder: > - Create a utility function getStrike(stock, time, offsetType, offsetValue).
   * It must first fetch the index price from SpotData.
   * It must round the index price to the nearest standard interval (50 for NIFTY, 100 for BANKNIFTY).
   * It returns the final TargetStrike.
3. Validator Integration: > - The "Validate" button must use the TargetStrike to find the correct row in the OptionsData table.
   * If the exact TargetStrike script is missing in OptionsData, return a "Data Gap" error rather than calculating wrong P&L.
________________


🚩 Critical Flaw to Avoid (The "Rounding" Trap)
In Indian markets (NSE), "ATM" can be interpreted two ways:
* Closest Strike: 19,523 -> 19,500.
* In-The-Money (ITM) Bias: Some traders want the strike below the current price for Calls.
My Suggestion: In your Indicator.csv form, add a small toggle: "Rounding Method: Closest / Floor / Ceiling." This small detail will make your SaaS look professional and "built by traders for traders."
Final Check:
1. User Upload: User will now upload three types of files:
   * Price Data (Options)
   * Indicator Data (Signals)
   * Spot Data (Index Prices) — New
2. Mapping: The field-mapping logic we built earlier will work for all three.
You are now fully architected. You have the database structure, the filtering logic, the signal-to-price mapping, and the ATM calculation math.